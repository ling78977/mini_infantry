# 基础格式化配置（控制单行宽度）
Language:        Cpp
ColumnLimit:     140  # 单行最大字符数（例如 80/100/120，默认80）

# 其他常用辅助配置（可选，根据项目需求调整）
IndentWidth:     2       # 缩进宽度（空格数）
UseTab:          Never   # 不使用制表符（用空格代替）
TabWidth:        2       # 若使用制表符，制表符宽度